# Old from homebrew - using nvm direct install now:
#
# source /usr/local/opt/nvm/nvm.sh

export NVM_DIR="/Users/bhughes/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm

# vi: ft=sh
